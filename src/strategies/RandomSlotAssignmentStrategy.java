package strategies;

import models.Gate;
import models.ParkingSlot;
import models.VehicleType;

public class RandomSlotAssignmentStrategy implements ParkingSlotAssignmentStrategy{
    @Override
    public ParkingSlot getParkingSlot(Gate gate, VehicleType vehicleType) {
        return null;
    }
}
